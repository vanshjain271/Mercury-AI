# Copy to .env.local and fill in. Get the publishable key by running the
# Mercury seed script on the backend (pnpm run backend:seed) - it creates
# one named "Mercury Buyer App" and prints its token.
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
