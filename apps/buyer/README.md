# Mercury Buyer app

The customer-facing half of Mercury AI - browsing, cart, Razorpay checkout, and an AI shopping assistant. See the [repository root README](../../README.md) for setup and the overall architecture.

```bash
pnpm run buyer:dev   # from the repo root, http://localhost:3000
```
